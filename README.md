# Standalone HTML Project

This project is a static HTML recreation of the supplied Vite/React/Figma export.

## Use
Open `index.html` in a browser. No build step, server, React runtime, or package install is required.

## Files
- `index.html` - static markup
- `styles.css` - generated CSS for the exported design classes
- `assets/` - local image assets

